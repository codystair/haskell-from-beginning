1 + -1 -- we need parentheses aroung -1
A == a -- we are referencing variables and not chars?
false || true -- need to be capitalized
if 'A' > 'a' then True -- need an else clause
'a' + 'b' -- can't add chars